export default {
    setIsLoading(state, value) {
        state.isLoading = value
    },
    setshowPosition(state, value) {
        state.showPosition = value
    },
    setIsAgree(state, value) {
        state.isAgree = true
    },
    setisshowAreement(state, value) {
        state.isshowAreement = value
    }
}
